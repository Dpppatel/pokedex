


const Pokemons = ({fetch, selections, data}) =>{
    //use .entry_number to get the id
    //use .pokemon_species.name to get the name
    //before fetch again, pass `https://pokeapi.co/api/v2/pokemon/${id};` to fetch instead of the link

    return
}

export default Pokemons;